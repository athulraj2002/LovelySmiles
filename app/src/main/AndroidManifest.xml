<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.raju.lovelysmiles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Home"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registration1" android:theme="@style/AppTheme.NoActionbar"/>
        <activity
            android:name=".Signup"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity android:name=".Registration2" android:theme="@style/AppTheme.NoActionbar" />
        <activity android:name=".registration3" android:theme="@style/AppTheme.NoActionbar"/>
        <activity android:name=".payment" android:theme="@style/AppTheme.NoActionbar"></activity>
    </application>

</manifest>